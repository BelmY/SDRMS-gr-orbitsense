<?xml version="1.0"?>
<block>
  <name>Statistics</name>
  <key>orbitsense_Statistics</key>
  <category>[orbitsense]</category>
  <import>import orbitsense</import>
  <make>orbitsense.Statistics($samp_rate, $signal_on, $modulation, $nf_estimation_time)</make>

	<callback>self.$(id).set_signal($signal_on)</callback>				
					
	<!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Sampling Rate</name>
    <key>samp_rate</key>
    <type>real</type>
  </param>

  <param>
    <name>Modulation</name>
    <key>modulation</key>
    <type>raw</type>
	</param>

	<param>
		<name>Signal On</name>
		<key>signal_on</key>
		<type>int</type>
	</param>

	<param>
		<name>NF estimation time (s)</name>
		<key>nf_estimation_time</key>
		<type>real</type>
	</param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex<!-- e.g. int, float, complex, byte, short, xxx_vector, ...--></type>
  </sink>

	<sink>
		<name>data_in</name>
		<type>message</type>
		<optional>1</optional>
	</sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex<!-- e.g. int, float, complex, byte, short, xxx_vector, ...--></type>
  </source>

</block>
